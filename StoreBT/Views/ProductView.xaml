<UserControl x:Class="StoreBT.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">  
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- ==================== HEADER BAR ==================== -->
        <Grid Margin="25,15,25,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- 🔙 Nút quay lại -->
            <Button Grid.Column="0"
                        Width="100"
                        Height="36"
                        Padding="10,0"
                        Background="#EFF6FF"
                        Foreground="#1D4ED8"
                        BorderBrush="#BFDBFE"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="Back_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE72B;" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="Quay lại"/>
                </StackPanel>
            </Button>
            <!-- 🏷️ Tiêu đề -->
            <TextBlock Grid.Column="1"
                           Text="📦  Quản lý sản phẩm"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#1E3A8A"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            <!-- ➕ Nút thêm mới -->
            <Button Grid.Column="2"
                        Width="120"
                        Height="36"
                        Background="#2563EB"
                        Foreground="White"
                        BorderBrush="#1E40AF"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="AddProduct_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="Thêm mới"/>
                </StackPanel>
            </Button>
        </Grid>
        <!-- ==================== THANH TÌM KIẾM ==================== -->
        <Border Grid.Row="1"
            Margin="20,20,20,0"
            CornerRadius="10"
            Background="White"
            BorderBrush="#CBD5E1"
            BorderThickness="2"
            Height="36">
                <Grid>
                    <TextBox x:Name="txtSearch"
                     BorderThickness="0"
                     Style="{StaticResource PlaceholderTextBox}"
                     Tag="Tìm kiếm"
                     Background="Transparent"
                     Padding="10,5,35,5"
                     TextChanged="txtSearch_TextChanged"/>

                <Button
                    Width="auto"
                    Height="28"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,2,0"
                    Background="Transparent"
                    BorderBrush="{x:Null}"
                    Cursor="Hand"
                    ToolTip="Quét mã vạch"
                    Click="btnScanBarcode_Click">
                    <Image Source="/Assets/Icons/scan.png"
                       Width="36"
                       Height="auto"
                       Stretch="Fill"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
                </Button>
            </Grid>
        </Border>

        <!-- ==================== DANH SÁCH SẢN PHẨM ==================== -->
        <DataGrid Grid.Row="2"
          AutoGenerateColumns="False"
          HeadersVisibility="Column"
          CanUserAddRows="False"
          RowHeight="40"
          ColumnHeaderHeight="40"
          Background="White"
          BorderBrush="#E2E8F0"
          AlternatingRowBackground="#F8FAFC"
          SelectionMode="Single"
          FontSize="14"
          IsReadOnly="True"
          x:Name="ProductGrid"
          Margin="20,20,20,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã vạch" Binding="{Binding Barcode}" Width="120"/>
                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Loại" Binding="{Binding Category}" Width="150"/>
                <DataGridTextColumn Header="Giá bán" Binding="{Binding Price, StringFormat={}{0:N0} ₫}" Width="120"/>
                <DataGridTextColumn Header="Tồn kho" Binding="{Binding Stock}" Width="100"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}" Width="150"/>

                <!-- 🧩 Cột Thao tác -->
                <DataGridTemplateColumn Header="Thao tác" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,10,0">

                                <!-- ✏️ Nút Sửa -->
                                <Button Width="26" Height="26"
                                Background="Transparent"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                ToolTip="Chỉnh sửa"
                                Click="Edit_Click">
                                    <Image Source="/Assets/Icons/edit.png" Width="18" Height="18"/>
                                </Button>

                                <!-- ❌ Nút Xóa -->
                                <Button Width="26" Height="26"
                                Background="Transparent"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                ToolTip="Xóa sản phẩm"
                                Click="Delete_Click">
                                    <Image Source="/Assets/Icons/delete.png" Width="18" Height="18"/>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Popup x:Name="ProductPopup"
       AllowsTransparency="True"
       Placement="Center"
       StaysOpen="False">
            <Border Background="#80000000" Padding="40" CornerRadius="8">
                <Border Background="White"
                CornerRadius="12"
                Padding="20"
                Width="500"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Thêm sản phẩm" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#1E3A8A"
                           Margin="0,0,0,10"
                           HorizontalAlignment="Center"/>

                        <!-- Form nhập -->
                        <StackPanel Margin="0,10,0,0">
                            <!-- 🔹 Mã vạch -->
                            <StackPanel Margin="0,5">
                                <TextBlock Text="Mã vạch" 
                                   FontWeight="SemiBold" 
                                   Foreground="#1E3A8A"
                                   Margin="0,0,0,4"/>
                                <Border CornerRadius="8"
                                BorderBrush="#CBD5E1"
                                BorderThickness="1"
                                Background="White">
                                    <Grid>
                                        <TextBox Style="{StaticResource PlaceholderTextBox}"
                                         x:Name="txtBarcode"
                                         Tag="Mã vạch"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         Padding="10,8,40,8"/>

                                        <Button x:Name="btnScanBarcode"
                                        Width="auto"
                                        Height="32"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Margin="0,0,5,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="Quét mã vạch"
                                        Click="btnScanBarcode_Click">
                                            <Image Source="/Assets/Icons/scan.png"
                                           Width="39"
                                           Height="auto"
                                           Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </StackPanel>

                            <!-- 🔹 Các input khác -->
                            <StackPanel Margin="0,5">
                                <TextBlock Text="Tên sản phẩm" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtName" Tag="Nhập tên sản phẩm" Padding="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,5">
                                <TextBlock Text="Loại sản phẩm" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtCategory" Tag="Nhập loại sản phẩm" Padding="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,5">
                                <TextBlock Text="Giá bán" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtPrice" Tag="Nhập giá bán" Padding="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,5">
                                <TextBlock Text="Tồn kho" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtStock" Tag="Nhập số lượng tồn" Padding="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,5">
                                <TextBlock Text="Mô tả" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" 
                                 x:Name="txtDescription"
                                 Tag="Thêm mô tả sản phẩm"
                                 Padding="10"
                                 Height="60"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Nút hành động -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button Content="Hủy"
                            Width="80"
                            Margin="0,0,10,0"
                            Background="White"
                            Click="CancelPopup_Click"/>
                            <Button Content="Lưu"
                            Width="80"
                            Background="#1E40AF"
                            Foreground="White"
                            Click="SaveProduct_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </Popup>
    </Grid>
</UserControl>
