<UserControl x:Class="StoreBT.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- ==================== HEADER BAR ==================== -->
        <Grid Margin="25,15,25,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- 🔙 Nút quay lại -->
            <Button Grid.Column="0"
                        Width="100"
                        Height="36"
                        Padding="10,0"
                        Background="#EFF6FF"
                        Foreground="#1D4ED8"
                        BorderBrush="#BFDBFE"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="Back_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE72B;" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="Quay lại"/>
                </StackPanel>
            </Button>
            <!-- 🏷️ Tiêu đề -->
            <TextBlock Grid.Column="1"
                           Text="👥  Quản lý khách hàng"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#1E3A8A"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            <!-- ➕ Nút thêm mới -->
            <Button Grid.Column="2"
                        Width="120"
                        Height="36"
                        Background="#2563EB"
                        Foreground="White"
                        BorderBrush="#1E40AF"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="AddCustomer_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="Thêm mới"/>
                </StackPanel>
            </Button>
        </Grid>
        <!-- ==================== THANH TÌM KIẾM ==================== -->
        <Border Grid.Row="1"
            Margin="20,20,20,0"
            CornerRadius="10"
            Background="White"
            BorderBrush="#CBD5E1"
            BorderThickness="2"
            Height="36">
            <Grid>
                <TextBox x:Name="txtSearch"
                     BorderThickness="0"
                     Style="{StaticResource PlaceholderTextBox}"
                     Tag="Tìm kiếm"
                     Background="Transparent"
                     Padding="10,5,35,5"
                     TextChanged="txtSearch_TextChanged"/>
            </Grid>
        </Border>

        <!-- ==================== DANH SÁCH SẢN PHẨM ==================== -->
        <DataGrid Grid.Row="2"
          AutoGenerateColumns="False"
          HeadersVisibility="Column"
          CanUserAddRows="False"
          RowHeight="40"
          ColumnHeaderHeight="40"
          Background="White"
          BorderBrush="#E2E8F0"
          AlternatingRowBackground="#F8FAFC"
          SelectionMode="Single"
          FontSize="14"
          IsReadOnly="True"
          x:Name="CustomerGrid"
          Margin="20,20,20,0">

            <DataGrid.Columns>
                <DataGridTextColumn ElementStyle="{StaticResource CenterTextCell}" Header="Tên khách hàng" Binding="{Binding Name}" Width="120"/>
                <DataGridTextColumn ElementStyle="{StaticResource CenterTextCell}" Header="Số điện thoại" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn ElementStyle="{StaticResource CenterTextCell}" Header="Địa chỉ" Binding="{Binding Address}" Width="2*"/>
                <DataGridTextColumn ElementStyle="{StaticResource CenterTextCell}" Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}" Width="150"/>

                <!-- 🧩 Cột Thao tác -->
                <DataGridTemplateColumn Header="Thao tác" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,10,0">

                                <!-- ✏️ Nút Sửa -->
                                <Button Width="26" Height="26"
                                Background="Transparent"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                ToolTip="Chỉnh sửa"
                                Click="Edit_Click">
                                    <Image Source="/Assets/Icons/edit.png" Width="18" Height="18"/>
                                </Button>

                                <!-- ❌ Nút Xóa -->
                                <Button Width="26" Height="26"
                                Background="Transparent"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                ToolTip="Xóa sản phẩm"
                                Click="Delete_Click">
                                    <Image Source="/Assets/Icons/delete.png" Width="18" Height="18"/>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Popup x:Name="CustomerPopup"
       AllowsTransparency="True"
       Placement="Center"
       StaysOpen="False">
            <Border Background="#80000000" Padding="40" CornerRadius="8">
                <Border Background="White"
                CornerRadius="12"
                Padding="20"
                Width="500"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Thêm khách hàng" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#1E3A8A"
                           Margin="0,0,0,10"
                           HorizontalAlignment="Center"/>

                        <StackPanel Margin="0,10,0,0">
                            <StackPanel Margin="0,5">
                                <TextBlock Text="Tên khách hàng" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtName" Tag="Nhập tên khách hàng" Padding="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,5">
                                <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtPhone" Tag="Nhập số điện thoại" Padding="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,5">
                                <TextBlock Text="Địa chỉ" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,4"/>
                                <TextBox Style="{StaticResource PlaceholderTextBox}" x:Name="txtAddress" Tag="Nhập địa chỉ" Padding="10"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button Content="Hủy"
                            Width="80"
                            Margin="0,0,10,0"
                            Background="White"
                            Click="CancelPopup_Click"/>
                            <Button Content="Lưu"
                            Width="80"
                            Background="#1E40AF"
                            Foreground="White"
                            Click="SaveCustomer_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </Popup>
    </Grid>
</UserControl>
